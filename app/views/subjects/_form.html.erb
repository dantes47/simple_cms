<%= error_messages(form.object) %>

<table summary="Subject from Fields">
  <tr>
     <th>Name</th>
        <td><%= form.text_field(:name) %></td>
     </tr>
     <tr>
     <th>Position</th>
        <td><%= form.text_field(:position) %></td>
     </tr>
     <tr>
     <th>Visible</th>
     <td><%= form.text_field(:visible) %></td>
  </tr>
</table>

